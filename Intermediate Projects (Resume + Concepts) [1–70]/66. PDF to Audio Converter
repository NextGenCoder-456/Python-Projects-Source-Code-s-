import pyttsx3
import PyPDF2

reader = PyPDF2.PdfReader(open("sample.pdf", "rb"))
text = ""
for page in reader.pages:
    text += page.extract_text()

engine = pyttsx3.init()
engine.say(text)
engine.runAndWait()
